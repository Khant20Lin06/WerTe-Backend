import { PrismaService } from '../../../infrastructure/database/prisma.service';
import { AuditService } from '../../audit/services/audit.service';
import { AuthenticatedUserEntity } from '../../auth/entities/authenticated-user.entity';
import { BranchesService } from '../../branches/services/branches.service';
import { AdjustInventoryDto } from '../dto/adjust-inventory.dto';
import { CreateMenuItemDto } from '../dto/create-menu-item.dto';
import { MenuItemDto } from '../dto/menu-item.dto';
import { MenuItemRuleProfileDto } from '../dto/menu-item-rule-profile.dto';
import { UpdateMenuItemDto } from '../dto/update-menu-item.dto';
import { MenuItemPolicyService } from '../policies/menu-item-policy.service';
import { MenusRepository } from '../repositories/menus.repository';
import { MenuItemInventoryService } from './menu-item-inventory.service';
import { MenusService } from './menus.service';
export declare class MerchantMenuItemsService {
    private readonly prisma;
    private readonly branchesService;
    private readonly menusService;
    private readonly menusRepository;
    private readonly menuItemPolicyService;
    private readonly auditService;
    private readonly menuItemInventoryService;
    constructor(prisma: PrismaService, branchesService: BranchesService, menusService: MenusService, menusRepository: MenusRepository, menuItemPolicyService: MenuItemPolicyService, auditService: AuditService, menuItemInventoryService: MenuItemInventoryService);
    listBranchItems(currentUser: AuthenticatedUserEntity, branchId: string): Promise<MenuItemDto[]>;
    getBranchItem(currentUser: AuthenticatedUserEntity, branchId: string, itemId: string): Promise<MenuItemDto>;
    listBranchItemRuleProfiles(currentUser: AuthenticatedUserEntity, branchId: string): Promise<MenuItemRuleProfileDto[]>;
    createBranchItem(currentUser: AuthenticatedUserEntity, branchId: string, payload: CreateMenuItemDto): Promise<MenuItemDto>;
    updateBranchItem(currentUser: AuthenticatedUserEntity, branchId: string, itemId: string, payload: UpdateMenuItemDto): Promise<MenuItemDto>;
    adjustBranchItemInventory(currentUser: AuthenticatedUserEntity, branchId: string, itemId: string, payload: AdjustInventoryDto): Promise<MenuItemDto>;
    deleteBranchItem(currentUser: AuthenticatedUserEntity, branchId: string, itemId: string): Promise<void>;
    private buildScopeSnapshot;
    private toScopeMode;
    private resolveOwnedBranch;
    private resolveOwnedItem;
    private resolveOptionalOwnedCategory;
    private toOptionalJson;
    private loadApprovedRuleStoreTypes;
    private resolveScopedStoreTypeIds;
    private normalizeStoreTypeIds;
    private resolveCreateEffectiveRuleStoreTypes;
    private resolveUpdateEffectiveRuleStoreTypes;
    private filterStoreTypesByIds;
    private resolveBranchDefaultRuleStoreTypes;
    private humanizeStoreTypeCode;
    private toJsonObject;
}
